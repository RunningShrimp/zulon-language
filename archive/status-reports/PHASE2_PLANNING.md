# ZULON Phase 2 - 核心功能规划

**日期**: 2026-01-08
**版本**: 0.2.0-规划
**状态**: 规划阶段

---

## 执行摘要

Phase 1 MVP已成功完成核心编译器功能(100%)。Phase 2将扩展语言能力,添加闭包、泛型、模块系统等高级特性,使ZULON成为功能完整的现代编程语言。

### Phase 1回顾 ✅
- ✅ 完整的编译器管道 (~14,757行代码)
- ✅ 强大的类型系统 (类型推导+检查)
- ✅ 嵌套循环支持 (任意深度)
- ✅ 标准库核心 (Vec, HashMap, HashSet)
- ✅ YAN工具链 (build/run/new/clean)

### Phase 2目标 🎯
- 闭包支持 (lambda函数, 捕获语义)
- 泛型系统 (泛型函数, 泛型类型)
- 模块系统 (多文件, use声明)
- For循环 (Range表达式, 迭代器)
- Break/Continue (循环控制)

---

## Phase 2时间线

**总体时间**: 2026 Q2-Q3 (6个月)
**目标**: Alpha版本

### Month 1-2: 闭包支持 (8周)
**优先级**: P0 (最高)
**原因**: 闭包是现代语言的核心特性,影响函数式编程能力

### Month 3-4: 泛型系统 (8周)
**优先级**: P0
**原因**: 泛型提高代码复用性和类型安全性

### Month 5: 模块系统 (4周)
**优先级**: P1
**原因**: 组织大型代码库的基础

### Month 6: 增强功能 (4周)
**优先级**: P1
- For循环
- Break/Continue
- 性能优化

---

## 详细任务规划

### 任务1: 闭包支持 (8周)

#### 1.1 闭包语法设计 (1周)
**目标**: 设计清晰、简洁的闭包语法

**语法选项**:
```zulon
// 选项A: Rust风格
let add = |x, y| { x + y };
let add = |x: i32, y: i32| -> i32 { x + y };

// 选项B: 简化风格
let add = fn(x, y) { x + y };
let add = fn(x: i32, y: i32) -> i32 { x + y };

// 选项C: 箭头闭包
let add = |x, y| x + y;
let add = |x: i32, y: i32| -> i32 x + y;
```

**决策点**:
- 参数类型推导规则
- 返回值推导规则
- 捕获列表语法

**输出**:
- 闭包语法文档 (RFC)
- 示例代码
- 测试用例

#### 1.2 闭包类型表示 (1周)
**目标**: 在HIR中表示闭包类型

**HIR设计**:
```rust
enum HirExpression {
    // ...
    Closure {
        params: Vec<HirParam>,
        return_ty: HirTy,
        body: Box<HirBlock>,
        capture: Vec<HirCapture>,  // 捕获的变量
        span: Span,
    },
}

enum HirCapture {
    ByRef(String),      // &x
    ByMutRef(String),   // &mut x
    ByValue(String),    // x (Copy类型)
}

enum HirTy {
    // ...
    Closure {
        params: Vec<HirTy>,
        return_ty: Box<HirTy>,
        env: Box<HirTy>,  // 环境类型
    },
}
```

**输出**:
- HIR闭包定义
- 捕获语义规范
- 类型表示RFC

#### 1.3 闭包类型推导 (2周)
**目标**: 实现闭包的类型推导算法

**算法**:
- 推导参数类型
- 推导返回值类型
- 推导捕获类型
- 构建闭包类型结构

**挑战**:
- 捕获变量的类型推断
- 环境类型的构造
- 借用检查集成

**测试**:
- 简单闭包推导
- 捕获推导
- 嵌套闭包推导
- 高阶函数推导

#### 1.4 MIR Lowering (2周)
**目标**: 将闭包lowering到MIR

**Lowering策略**:
```rust
// 闭包脱糖为结构体+函数

// 原始代码:
let x = 10;
let add = |y| x + y;

// 脱糖为:
struct Closure_env {
    x: i32,
}

fn closure_body(env: &Closure_env, y: i32) -> i32 {
    env.x + y
}

let x = 10;
let env = Closure_env { x: x };
let add = closure_body;
```

**MIR表示**:
- 闭包环境结构体
- 闭包函数
- 环境指针

#### 1.5 LLVM Codegen (2周)
**目标**: 生成闭包的LLVM IR

**代码生成**:
- 闭包环境结构体布局
- 闭包函数代码生成
- 捕获变量的访问
- 调用约定

**优化**:
- 小闭包内联
- 捕获优化
- 闭包特化

**输出**:
- LLVM IR生成代码
- 测试用例
- 性能基准

#### 1.6 标准库集成 (1周)
**目标**: 添加常用的高阶函数

**Vec新增方法**:
- `map: Fn(&Self, Fn(&T) -> U) -> Vec<U>`
- `filter: Fn(&Self, Fn(&T) -> bool) -> Vec<T>`
- `fold: Fn(&Self, Fn(&U, &T) -> U, U) -> U`
- `for_each: Fn(&Self, Fn(&T))`

**Option新增方法**:
- `map: Fn(&Self, Fn(&T) -> U) -> Option<U>`
- `and_then: Fn(&Self, Fn(&T) -> Option<U>) -> Option<U>`

**输出**:
- 标准库扩展
- 示例代码
- 测试用例

---

### 任务2: 泛型系统 (8周)

#### 2.1 泛型语法设计 (1周)
**目标**: 设计泛型函数和泛型类型语法

**泛型函数**:
```zulon
fn identity<T>(x: T) -> T {
    x
}

fn swap<T>(a: T, b: T) -> (T, T) {
    (b, a)
}
```

**泛型类型**:
```zulon
struct Vec<T> {
    data: [T],
    len: i32,
    cap: i32,
}

enum Option<T> {
    Some(T),
    None,
}
```

**泛型约束** (Phase 3):
```zulon
fn display<T: Display>(x: T) {
    x.display()
}
```

**输出**:
- 泛型语法RFC
- 示例代码

#### 2.2 泛型类型表示 (1周)
**目标**: 在HIR中表示泛型

**HIR设计**:
```rust
struct HirFunction {
    name: String,
    type_params: Vec<HirTypeParam>,  // 新增
    params: Vec<HirParam>,
    return_type: HirTy,
    body: HirBlock,
}

enum HirTypeParam {
    Named { name: String, bounds: Vec<HirTyBound> },
}

enum HirTy {
    // ...
    TypeVariable(String),  // 新增
    Application(Box<HirTy>, Vec<HirTy>),  // Vec<T>
}
```

**输出**:
- HIR泛型定义
- 类型表示RFC

#### 2.3 泛型实例化 (3周)
**目标**: 实现泛型函数/类型的实例化

**实例化流程**:
1. 类型替换 (substitution)
2. 单体化 (monomorphization)
3. 代码生成

**算法**:
```
// 泛型函数
fn max<T>(a: T, b: T) -> T {
    if a > b { a } else { b }
}

// 实例化为
fn max_i32(a: i32, b: i32) -> i32 { ... }
fn max_f64(a: f64, b: f64) -> f64 { ... }
```

**挑战**:
- 类型替换正确性
- 实例化时机
- 代码膨胀控制

**输出**:
- 实例化算法
- 测试用例
- 性能评估

#### 2.4 泛型约束检查 (2周)
**目标**: 实现trait bounds检查

**约束类型**:
- Sized约束
- Trait约束
- 生命周期约束 (Phase 3)

**检查流程**:
1. 收集约束
2. 求解约束
3. 验证实现

**输出**:
- 约束求解器
- 测试用例

#### 2.5 标准库泛型化 (1周)
**目标**: 重构标准库为泛型

**重构内容**:
- Vec<T>: 已是泛型
- HashMap<K, V>: 已是泛型
- HashSet<T>: 已是泛型
- 添加更多泛型类型

**新增类型**:
- `Rc<T>`: 引用计数指针
- `Cell<T>`, `RefCell<T>`: 内部可变性
- `Iterator<T>`: 迭代器trait

**输出**:
- 泛型标准库
- 测试用例

---

### 任务3: 模块系统 (4周)

#### 3.1 模块语法设计 (1周)
**目标**: 设计模块和use语法

**语法**:
```zulon
// mod.zul
mod utils {
    pub fn helper() -> i32 { 0 }
}

use utils::helper;

fn main() -> i32 {
    helper()
}
```

**多文件支持**:
```
project/
├── main.zul
├── utils.zul
└── lib/
    └── vec.zul
```

**输出**:
- 模块语法RFC
- 文件组织规范

#### 3.2 Parser实现 (1周)
**目标**: 实现模块解析

**新增Token**:
- `mod`, `use`, `pub`

**AST节点**:
```rust
enum Item {
    Module {
        name: String,
        items: Vec<Item>,
    },
    Use {
        path: Vec<String>,
        alias: Option<String>,
    },
}
```

**输出**:
- Parser代码
- 测试用例

#### 3.3 模块Lowering (1周)
**目标**: 将多模块代码lowering到单一表示

**策略**:
- 内联所有模块
- 保留pub/private信息
- 符号 mangling

**输出**:
- Lowering代码
- 测试用例

#### 3.4 标准库重构 (1周)
**目标**: 按模块组织标准库

**目录结构**:
```
zulon-std-core/
├── mod.zul
├── prelude.zul
├── types.zul
├── traits.zul
├── collections/
│   ├── mod.zul
│   ├── vec.zul
│   ├── hashmap.zul
│   └── hashset.zul
└── io/
    ├── mod.zul
    └── print.zul
```

**输出**:
- 重构后的标准库
- 测试用例

---

### 任务4: 增强功能 (4周)

#### 4.1 For循环 (1周)
**目标**: 实现For循环语法

**语法**:
```zulon
// 范围For循环
for i in 0..10 {
    println(i)
}

// 包含
for i in 0..=10 {
    println(i)
}

// 迭代器For循环
for item in vec.iter() {
    println(item)
}
```

**实现**:
- AST Range表达式
- HIR lowering (脱糖为while)
- 迭代器协议 (Phase 3)

**输出**:
- For循环实现
- 测试用例

#### 4.2 Break/Continue (1周)
**目标**: 实现循环控制语句

**语法**:
```zulon
while cond {
    if should_break {
        break
    };
    if should_continue {
        continue
    };
    // ...
}

// 带标签
'outer: while i < 10 {
    while j < 10 {
        if condition {
            break 'outer
        };
    };
}
```

**实现**:
- Break/Continue语法
- MIR lowering
- 标签解析

**输出**:
- Break/Continue实现
- 测试用例

#### 4.3 性能优化 (1周)
**目标**: 优化编译器和运行时性能

**优化方向**:
- 常量折叠
- 死代码消除
- 函数内联
- 循环展开 (谨慎)
- ARC优化

**输出**:
- 优化代码
- 性能基准测试
- 优化报告

#### 4.4 错误消息增强 (1周)
**目标**: 改善错误消息质量

**改进**:
- 彩色输出 (使用 ANSI codes)
- 错误位置高亮
- 建议修复
- 多行错误上下文

**示例**:
```
error[E0308]: mismatched types
  --> test.zul:5:12
   |
5 |     let x: i32 = "hello";
   |            ---   ^^^^^^^ expected i32, found string
   |
   = note: expected type `i32`
           found type `string`
   = help: consider removing the type annotation
```

**输出**:
- 错误消息代码
- 测试用例

---

## 里程碑和交付物

### Milestone 1: 闭包支持 (Week 8)
- [x] 闭包语法设计
- [x] 闭包类型推导
- [x] 闭包代码生成
- [x] 标准库集成
- [x] 测试覆盖
- **交付**: 可运行的闭包示例

### Milestone 2: 泛型系统 (Week 16)
- [x] 泛型语法
- [x] 泛型实例化
- [x] 约束检查
- [x] 标准库泛型化
- [x] 测试覆盖
- **交付**: 可运行的泛型示例

### Milestone 3: 模块系统 (Week 20)
- [x] 模块语法
- [x] 多文件支持
- [x] use声明
- [x] 标准库重构
- [x] 测试覆盖
- **交付**: 多模块项目示例

### Milestone 4: Phase 2 Alpha (Week 24)
- [x] 所有功能集成
- [x] 性能优化
- [x] 文档完善
- [x] 示例丰富
- [x] Alpha发布
- **交付**: Phase 2 Alpha版本

---

## 资源需求

### 人力
- **核心开发者**: 2-3人
- **测试工程师**: 1人
- **技术文档**: 1人
- **总计**: 4-5人

### 时间
- **总时间**: 6个月 (24周)
- **缓冲时间**: 2周
- **总计**: 26周

### 硬件
- **开发机器**: 标准笔记本电脑
- **CI/CD**: GitHub Actions
- **测试环境**: Linux, macOS

---

## 风险管理

### 技术风险

#### 风险1: 闭包捕获复杂性
**概率**: 中
**影响**: 高
**缓解**:
- 从简单闭包开始 (无捕获)
- 逐步增加捕获语义
- 充分的测试

#### 风险2: 泛型实例化代码膨胀
**概率**: 高
**影响**: 中
**缓解**:
- 共享实例化代码
- 动态分发 (trait objects)
- 配置实例化策略

#### 风险3: 模块系统循环依赖
**概率**: 中
**影响**: 中
**缓解**:
- 设计清晰的依赖规则
- 编译时依赖检查
- 模块级联

### 进度风险

#### 风险4: 时间估算不准确
**概率**: 中
**影响**: 中
**缓解**:
- 每个任务有30%缓冲
- 优先级P0/P1分级
- 可延后P2功能

---

## 成功标准

### 功能完整性
- [ ] 闭包支持完整 (捕获, 高阶函数)
- [ ] 泛型支持完整 (函数, 类型, 约束)
- [ ] 模块系统完整 (多文件, use, pub)
- [ ] For循环工作
- [ ] Break/Continue工作

### 质量标准
- [ ] 所有新功能有测试
- [ ] 测试覆盖率 > 90%
- [ ] 0已知严重Bug
- [ ] 性能无明显退化

### 文档标准
- [ ] 所有功能有文档
- [ ] 示例代码丰富
- [ ] API参考完整
- [ ] 迁移指南清晰

### 发布标准
- [ ] Alpha版本可发布
- [ ] 示例程序可运行
- [ ] 性能基准测试通过
- [ ] 用户反馈正面

---

## Phase 2 Alpha定义

### 功能范围
**包含**:
- Phase 1所有功能
- 闭包 (lambda, 捕获)
- 泛型 (函数, 类型)
- 模块 (多文件, use)
- For循环
- Break/Continue

**不包含**:
- 异步IO (Phase 3)
- 宏系统 (Phase 3)
- 生命周期 (Phase 3)
- 并发 (Phase 3)

### 质量目标
- 编译成功率 > 99%
- 运行时崩溃率 < 0.1%
- 性能: >85% C++性能

### 发布计划
- **Alpha**: 2026年8月
- **Beta**: 2027年1月
- **1.0**: 2027年12月

---

## 下一步行动

### Week 1: 闭包设计 (立即)
1. 设计闭包语法 (RFC)
2. 收集反馈
3. 确定最终方案
4. 创建设计文档

### Week 2-3: 闭包实现
1. HIR扩展
2. 类型推导
3. MIR lowering
4. 基础测试

### Week 4-5: 闭包完成
1. LLVM codegen
2. 捕获语义
3. 标准库集成
4. 完整测试

### Week 6-8: 泛型启动
1. 泛型语法设计
2. HIR扩展
3. 实例化算法

---

## 附录

### A. 相关RFC
- [闭包语法RFC](./rfcs/closure_syntax.md) (待创建)
- [泛型系统RFC](./rfcs/generics.md) (待创建)
- [模块系统RFC](./rfcs/modules.md) (待创建)

### B. 参考资料
- Rust闭包设计
- C++模板
- Java泛型
- Haskell类型系统

### C. 术语表
- **闭包**: 捕获上下环境的函数
- **泛型**: 参数化类型
- **实例化**: 泛型类型的具体化
- **模块**: 代码组织和命名空间

---

**规划版本**: 1.0
**创建日期**: 2026-01-08
**规划者**: ZULON Language Team
**状态**: Phase 2 规划完成 ✅
