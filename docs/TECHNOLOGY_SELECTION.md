# ZULON 技术选型文档

**版本**: 1.0
**日期**: 2026-01-07
**作者**: ZULON Language Team
**状态**: 设计阶段

---

## 目录

1. [概述](#概述)
2. [编译器后端选型](#编译器后端选型)
3. [运行时选型](#运行时选型)
4. [标准库选型](#标准库选型)
5. [工具链选型](#工具链选型)
6. [第三方依赖](#第三方依赖)
7. [平台支持](#平台支持)
8. [开发工具选型](#开发工具选型)
9. [风险评估](#风险评估)

---

## 概述

### 文档目标

本文档详细说明 ZULON 语言开发过程中的技术选型决策，包括：

- 技术选项的对比分析
- 选择特定技术的原因
- 替代方案的考虑
- 潜在的风险和缓解措施

### 选型原则

1. **性能优先**: 不牺牲关键路径的性能
2. **成熟度**: 选择经过验证的技术
3. **可维护性**: 代码库长期可维护
4. **社区支持**: 活跃的社区和生态系统
5. **许可证**: 兼容的开源许可证

---

## 编译器后端选型

### 选项对比

| 后端 | 优势 | 劣势 | 性能 | 成熟度 |
|------|------|------|------|--------|
| **LLVM** | 成熟、优化多、后端多 | 复杂、编译慢 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Cranelift** | 编译快、易于调试 | 后端少、优化少 | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **GCC** | 后端多、优化强 | API复杂、C++ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **自研** | 完全控制 | 开发成本高 | ⭐⭐⭐⭐ | ⭐ |

### 选择：LLVM

**理由**：

1. **成熟度**: LLVM 是工业标准的编译器基础设施，被 Rust、Swift、Kotlin 等语言使用
2. **优化**: 丰富的优化 Pass，支持 SSA、内联、向量化等
3. **后端支持**: 支持 x86、ARM、RISC-V、WebAssembly 等多种架构
4. **社区**: 庞大的社区和丰富的文档
5. **工具链**: 完善的工具链（llc、opt、lldb）

**LLVM 版本**: 15.0+

**集成方式**:

```rust
// 使用 LLVM 的 C API (FFI)
pub struct LLVMContext {
    pub context: LLVMContextRef,
    pub module: LLVMModuleRef,
    pub builder: LLVMBuilderRef,
}

impl LLVMContext {
    pub fn new(name: &str) -> Self {
        unsafe {
            let context = LLVMContextCreate();
            let module = LLVMModuleCreateWithName(
                name.as_str().as_ptr() as *const i8,
                context,
            );
            let builder = LLVMCreateBuilderInContext(context);

            LLVMContext {
                context,
                module,
                builder,
            }
        }
    }
}
```

### 替代方案：Cranelift（未来）

**使用场景**:
- 快速编译（开发模式）
- JIT 编译
- 嵌入式设备（资源受限）

**集成计划**:
- 2028年：开始集成 Cranelift 作为可选后端
- 2029年：支持开发模式使用 Cranelift
- 2030年：评估 Cranelift 作为主要后端的可行性

---

## 运行时选型

### 内存管理

#### 选项对比

| 方案 | 优势 | 劣势 | 性能 | 复杂度 |
|------|------|------|------|--------|
| **纯ARC** | 简单、实时 | 循环引用、开销 | ⭐⭐⭐ | 低 |
| **GC** | 自动、安全 | 暂停、不可预测 | ⭐⭐⭐ | 中 |
| **ARC+GC** | 混合、灵活 | 复杂 | ⭐⭐⭐⭐ | 高 |
| **Regions** | 批量释放 | 局限性 | ⭐⭐⭐⭐ | 高 |

#### 选择：Tree Borrows + ARC

**架构**:

```
编译期分析                运行时支持
    │                        │
    ▼                        ▼
Tree Borrows  ──────────►  ARC 引用计数
(借用检查)              (自动管理)
    │                        │
    ▼                        ▼
 简化借用              循环检测
  生命周期              弱引用
```

**理由**:

1. **编译期优化**: Tree Borrows 模型在编译期简化借用检查
2. **运行时安全**: ARC 提供自动内存管理，无需手动 free
3. **性能**: 逃逸分析后，大部分值在栈上分配
4. **简单性**: 比纯 GC 更可预测，比纯手动更安全

**实现**:

```rust
pub enum MemoryStrategy {
    // 栈分配（逃逸分析确定）
    Stack {
        offset: usize,
    },

    // 堆分配（ARC）
    Heap {
        ref_count: Arc<usize>,
    },

    // 静态分配
    Static,
}
```

### 并发运行时

#### 选项对比

| 方案 | 优势 | 劣势 | 性能 | 成熟度 |
|------|------|------|------|--------|
| **Thread-per-Core** | 简单、OS调度 | 线程重、上下文切换 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Goroutine-style** | 轻量、高效 | 需要运行时 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Async/Await** | 非阻塞IO | 学习曲线 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Actor** | 隔离、安全 | 消息开销 | ⭐⭐⭐ | ⭐⭐⭐ |

#### 选择：混合模型

```
1. 系统线程 (OS Thread)
   - 少量长时间运行的任务
   - 阻塞操作

2. 轻量任务 (Task)
   - 大量短时任务
   - 并发计算

3. 异步IO (Async/Await)
   - 网络操作
   - 文件IO

4. Actor 模型
   - 状态隔离
   - 消息传递
```

**实现**:

```rust
// 任务调度器
pub struct TaskScheduler {
    pub workers: Vec<Worker>,
    pub task_queue: Arc<Queue<Task>>,
    pub steal_queues: Vec<Arc<StealQueue<Task>>>,
}

// 异步运行时
pub struct AsyncRuntime {
    pub reactor: Reactor,
    pub executor: Executor,
}

// Actor 系统
pub struct ActorSystem {
    pub actors: HashMap<ActorId, Box<dyn Actor>>,
    pub mailbox: Arc<Mailbox>,
}
```

---

## 标准库选型

### HTTP 框架

#### 选项对比

| 框架 | 性能 | 易用性 | 生态系统 |
|------|------|--------|----------|
| **Hyper** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Actix-web** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **自研** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐ |

#### 选择：基于 Hyper 的封装

**理由**:

1. **性能**: Hyper 是最快的 Rust HTTP 库
2. **类型安全**: 强类型保证
3. **异步**: 原生支持 async/await
4. **成熟**: 被广泛使用和验证

**设计**:

```rust
pub use hyper::{Body, Request, Response};

pub struct Server {
    pub routes: Router,
}

impl Server {
    pub fn new() -> Self {
        Server {
            routes: Router::new(),
        }
    }

    pub fn get<F>(&mut self, path: &str, handler: F) -> &mut Self
    where
        F: Fn(Request<Body>) -> Response<Body> + Send + Sync + 'static,
    {
        self.routes.add_route(Method::GET, path, handler);
        self
    }

    pub async fn serve(self, addr: SocketAddr) -> Result<(), Error> {
        let make_service = MakeServiceService::new(self.routes);
        let server = Hyper::bind(addr).serve(make_service);
        server.await?;
        Ok(())
    }
}
```

### 数据库驱动

#### 选择：SQLx

**理由**:

1. **编译时检查**: SQL 查询在编译期验证
2. **类型安全**: 自动类型推导
3. **异步**: 原生 async 支持
4. **多数据库**: 支持 PostgreSQL, MySQL, SQLite

**示例**:

```rust
use sqlx::{PgPool, query};

pub struct User {
    pub id: i32,
    pub name: String,
    pub email: String,
}

pub async fn get_user(pool: &PgPool, id: i32) -> Result<User, Error> {
    let user = query!(
        "SELECT id, name, email FROM users WHERE id = $1"
        id
    )
    .fetch_one(pool)
    .await?;

    Ok(user)
}
```

### 序列化框架

#### 选择：Serde

**理由**:

1. **性能**: 零成本抽象
2. **易用**: derive 宏自动实现
3. **格式支持**: JSON, YAML, TOML, MessagePack 等
4. **社区标准**: Rust 生态事实标准

**示例**:

```rust
use serde::{Serialize, Deserialize};

#[derive(Serialize, Deserialize)]
pub struct User {
    pub id: i32,
    pub name: String,
    pub email: String,
}

let json = serde_json::to_string(&user)?;
let user: User = serde_json::from_str(&json)?;
```

---

## 工具链选型

### 包管理器

#### 参考模型：Cargo (Rust)

**核心功能**:

1. **依赖管理**: 声明依赖、版本解析
2. **构建系统集成**: 自动编译依赖
3. **工作空间**: 支持多包项目
4. **包注册表**: crates.io 风格

**实现**: zpm (ZULON Package Manager)

```rust
pub struct Package {
    pub name: String,
    pub version: Version,
    pub dependencies: Vec<Dependency>,
    pub source: GitSource | RegistrySource,
}

pub struct PackageRegistry {
    pub index: HashMap<String, Vec<PackageVersion>>,
    pub storage: Box<dyn Storage>,
}
```

### 构建系统

#### 参考模型：Cargo + Bazel

**功能**:

1. **增量编译**: 只重新编译修改的文件
2. **并行编译**: 多核并行
3. **依赖跟踪**: 自动跟踪文件依赖
4. **缓存**: 编译结果缓存

**实现**: zbuild

```rust
pub struct BuildGraph {
    pub nodes: HashMap<NodeId, Node>,
    pub edges: Vec<Edge>,
}

pub struct Builder {
    pub graph: BuildGraph,
    pub cache: BuildCache,
    pub parallel: Parallelism,
}

impl Builder {
    pub fn build(&mut self, target: &Target) -> Result<(), BuildError> {
        let order = self.graph.topological_order()?;

        if self.parallel == Parallelism::Full {
            self.build_parallel(&order)?;
        } else {
            for node in order {
                self.build_node(&node)?;
            }
        }

        Ok(())
    }
}
```

### 代码格式化

#### 选择：自定义格式化器

**参考**: rustfmt, prettier

**功能**:

1. **一致性**: 统一代码风格
2. **可配置**: 支持自定义格式选项
3. **幂等**: 多次运行结果一致
4. **快速**: 大文件快速格式化

**配置**:

```toml
# zfmt.toml
max_width = 100
indent_width = 4
tab_spaces = 4
hard_tabs = false
trailing_comma = true
reorder_imports = true
```

---

## 第三方依赖

### 依赖管理原则

1. **最小化**: 只依赖必要的库
2. **审查**: 安全审计所有依赖
3. **兼容性**: 许可证兼容
4. **维护**: 优先选择活跃维护的项目

### 核心依赖

| 依赖 | 版本 | 用途 | 许可证 |
|------|------|------|--------|
| LLVM | 15.0+ | 编译器后端 | Apache 2.0 |
| Rust | 1.70+ | 编译器实现 | MIT/Apache 2.0 |
| serde | 1.0+ | 序列化 | MIT/Apache 2.0 |
| sqlx | 0.7+ | 数据库 | Apache 2.0 |
| hyper | 0.14+ | HTTP | MIT |
| tokio | 1.0+ | 异步运行时 | MIT |

### 依赖风险评估

**低风险**:
- LLVM: 成熟、广泛使用
- Rust: 系统语言，稳定
- serde: Rust 事实标准

**中风险**:
- sqlx: 快速发展，API可能变化
- hyper: 版本更新频繁

**缓解措施**:
- 锁定依赖版本
- 定期更新和测试
- 关注安全公告

---

## 平台支持

### Tier 1 平台（完全支持）

| 平台 | 架构 | 状态 | 优先级 |
|------|------|------|--------|
| Linux | x86_64 | ✅ 支持 | P0 |
| Linux | aarch64 | ✅ 支持 | P0 |
| macOS | x86_64 | ✅ 支持 | P0 |
| macOS | aarch64 (Apple Silicon) | ✅ 支持 | P0 |
| Windows | x86_64 | ✅ 支持 | P1 |

### Tier 2 平台（实验性支持）

| 平台 | 架构 | 状态 | 优先级 |
|------|------|------|--------|
| Windows | aarch64 | 🔄 实验中 | P2 |
| Android | aarch64 | 🔄 计划中 | P2 |
| iOS | aarch64 | 🔄 计划中 | P2 |

### Tier 3 平台（未来支持）

| 平台 | 架构 | 状态 | 优先级 |
|------|------|------|--------|
| WebAssembly | wasm32 | 🔄 开发中 | P1 |
| RISC-V | riscv64 | 🔄 计划中 | P3 |
| FreeBSD | x86_64 | 🔄 计划中 | P3 |

### 平台抽象层

```rust
pub mod platform {
    #[cfg(target_os = "linux")]
    pub use self::linux as os;

    #[cfg(target_os = "macos")]
    pub use self::macos as os;

    #[cfg(target_os = "windows")]
    pub use self::windows as os;
}

pub trait OS {
    fn page_size() -> usize;
    fn alloc_executable(size: usize) -> *mut u8;
    fn current_thread() -> ThreadId;
}
```

---

## 开发工具选型

### 版本控制

**选择**: Git

**理由**:
- 行业标准
- 分布式
- 强大的分支模型
- GitHub/GitLab 集成

### CI/CD

**选择**: GitHub Actions

**理由**:
- 原生 GitHub 集成
- 免费用于开源项目
- 丰富的 Actions 生态
- 支持自托管 Runner

**配置**:

```yaml
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v3

      - name: Install LLVM
        run: |
          sudo apt-get install -y llvm-15-dev

      - name: Build
        run: |
          cargo build --release

      - name: Test
        run: |
          cargo test --all

      - name: Bench
        run: |
          cargo bench
```

### 文档生成

**选择**: rustdoc + mdBook

**理由**:
- rustdoc: 从代码注释生成 API 文档
- mdBook: 创建书籍风格文档

### 性能分析

**选择**:
- Flamegraph: 性能火焰图
- perf: Linux 性能分析
- Instruments: macOS 性能工具

---

## 风险评估

### 技术风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| LLVM API 变更 | 中 | 高 | 使用稳定版本，关注 LLVM RFC |
| 性能不达标 | 低 | 高 | 基准测试，优化关键路径 |
| 内存管理bug | 中 | 高 | 完善测试，形式化验证 |
| 跨平台问题 | 高 | 中 | 抽象层，持续集成 |

### 项目风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 开发进度延误 | 中 | 中 | MVP优先，迭代开发 |
| 生态不成熟 | 高 | 高 | 早期投入工具链开发 |
| 社区不够大 | 高 | 中 | 开源宣传，降低学习曲线 |
| 资金不足 | 中 | 高 | 寻求赞助、企业合作 |

### 依赖风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| LLVM 维护中断 | 低 | 高 | 多后端支持（Cranelift） |
| Rust 生态系统变化 | 中 | 中 | 锁定版本，定期评估 |
| 关键依赖停止维护 | 低 | 中 | 积极参与社区，准备 fork |

---

## 决策记录

### DC-001: 选择 LLVM 作为编译器后端

**日期**: 2026-01-07

**状态**: 已接受

**背景**: 需要选择可靠的编译器后端

**选项**:
1. LLVM
2. Cranelift
3. GCC
4. 自研

**决策**: 选择 LLVM

**理由**:
- 成熟度高，被 Rust、Swift 等验证
- 优化丰富，支持多种后端
- 社区活跃，文档完善
- 工具链完整

**后果**:
- 编译器复杂度增加
- 编译时间可能较慢

**缓解措施**:
- 未来集成 Cranelift 作为快速编译选项
- 增量编译缓解编译时间问题

### DC-002: 选择 Tree Borrows + ARC 内存模型

**日期**: 2026-01-07

**状态**: 已接受

**背景**: 需要平衡性能和安全性

**决策**: Tree Borrows (编译期) + ARC (运行时)

**理由**:
- 编译期简化借用检查
- 运行时自动管理内存
- 性能接近手动管理
- 比纯 GC 更可预测

**风险**: 循环引用

**缓解措施**:
- 弱引用支持
- 循环检测算法
- 开发者工具检测

### DC-003: 选择 Rust 实现编译器

**日期**: 2026-01-07

**状态**: 已接受

**背景**: 需要选择编译器实现语言

**决策**: 使用 Rust

**理由**:
- 内存安全，减少编译器 bug
- 性能接近 C++
- 丰富的库支持
- 模式匹配、错误处理等现代特性

**风险**: 学习曲线

**缓解措施**:
- 团队培训
- 详细文档
- 最佳实践指南

---

## 未来评估

### 需要定期评估的技术

1. **LLVM 替代方案**: 每年评估 Cranelift、GCC JIT 等方案
2. **内存管理**: 根据性能数据调整 ARC 策略
3. **并发模型**: 评估新的并发模型（如 async/await）
4. **平台支持**: 根据用户需求调整平台优先级

### 评估标准

- 性能基准测试结果
- 社区反馈和需求
- 技术发展趋势
- 维护成本

---

**文档版本**: 1.0
**最后更新**: 2026-01-07
**维护者**: ZULON Language Team
