// 05_while_loop.zl - While loops
// Demonstrates looping with while

fn sum_range(n: i32) -> i32 {
    let mut sum = 0;
    let mut i = 0;
    while i < n {
        sum = sum + i;
        i = i + 1
    };
    sum
}

fn main() -> i32 {
    sum_range(10)
}
